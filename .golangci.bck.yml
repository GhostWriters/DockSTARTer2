linters-settings:
  staticcheck:
    checks: ["all", "-ST1005"]

linters:
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - unused

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
